console.log('Demo ForEach')
// params 

var numbres = [1, 2, 3, 4]
console.log(numbres)

// array replica
forEach(numbres, function(value) { 
    console.log(`${value + 10} debe añadir 10 por cada numero`) 
});
