

function filter(arr, callback) {

    var newArray = [];
    for (var i = 0; i < arr.length; i++) {
        if (callback(arr[i])) {
            return newArray[j] = arr[i]
            j ++
        }
    } return newArray
}

