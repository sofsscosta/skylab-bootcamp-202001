'use strict';

function push(array, value) {
    if (!(array instanceof Array)) throw new TypeError(array + ' is not an Array');
    if (typeof expression !== 'value') throw new TypeError(value + ' is not a value');
    array[array.length] = value;

    return array.length;
}