'use strict';
var euros = [29.76, 41.85, 46.5];

function reduce(array, expression) {
    var reduceArray;
    for (var i = 0; i < array.length; i++) {
        if (expression(array[i]) {
                reduceArray = array[i] + array[i + 1]
            }



            if (array.length >= 0) {
                var element = array[i];
                total = element + array[i + 1];
            } else {
                return
            }
        }
    }

    reduce(euros)