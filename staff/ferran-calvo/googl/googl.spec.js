

describe('Herramienta Googl', function()  {

/*     it ('Should return an array with objects', function(done){
        googl('pepito', function(results){
            expect(results instanceof Array).toBe(true);
            results.forEach(result => {
                expect(result instanceof Object).toBe(true);
            });
            done();
        })
    })

    it ('Should return an array with objects with title and description', function(done){
        googl('jacinto', function(results){
            for(var i = 0; i < results.length; i++){
                   
                
            expect(typeof results[i].title).toBe("string")
            expect(typeof results[i].description).toBe("string")
            }
            done();
        })
    })

    it('should fail if query is not a string', function(){

            expect(function(){
            googl(54, function(results){})}).toThrowError(TypeError, '54 is not a string')
        })
     */
    
     it ('should fail')
});