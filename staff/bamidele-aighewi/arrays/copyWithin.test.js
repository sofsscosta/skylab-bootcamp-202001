describe('copyWithin', function(){
    it('should initialize method', function () {
        assert(typeof copyWithin === 'function', 'Function method expected got ' + (typeof copyWithin) + ' instead')
    });

    // it('')
})