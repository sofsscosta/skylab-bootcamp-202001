const chatServer = require('./chat-server')

describe('chatServer', () => {
    beforeEach(done => {
        chatServer()
        done()
    })
    it('should succedd on creating a new server', () => {
        expect()
    })
})