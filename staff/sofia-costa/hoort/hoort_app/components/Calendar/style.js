import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    message_container: {
        flexDirection: 'row',
        alignSelf: 'center',
    },
    message: {
        fontSize: 40
    }
})