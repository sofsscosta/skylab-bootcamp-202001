import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    message_container: {
        flexDirection: 'row',
        alignSelf: 'center',
        marginHorizontal: 20,
        flexWrap: 'wrap',
        alignItems: 'center',
        justifyContent: 'center',
    },
    message: {
        fontSize: 20,
    }
})